#Install Terraform on MacOS

brew tap hashicorp/tap

brew install hashicorp/tap/terraform

#check install
terraform --help

#Install AWS CLI

#Terraform commands
terraform init
terraform apply
terraform destroy